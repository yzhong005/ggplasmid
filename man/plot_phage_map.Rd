% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_phage_map}
\alias{plot_phage_map}
\title{Draw a phage map}
\usage{
plot_phage_map(
  annotation = NULL,
  gbk = NULL,
  fasta = NULL,
  skew_table = NULL,
  output = NULL,
  layout = c("circular", "linear"),
  phage_topology = c("circular", "linear"),
  name = NULL,
  genome_length = NULL,
  region_start = NULL,
  region_end = NULL,
  feature_types = c("CDS", "tRNA", "rRNA", "tmRNA", "ncRNA", "rep_origin",
    "mobile_element"),
  label_mode = c("auto", "product", "gene"),
  palette = "npg",
  gene_highlight = NULL,
  rows = 4,
  genome_line_num = NULL,
  plot_line_num = NULL,
  show_gc_skew = TRUE,
  show_labels = TRUE,
  label_unknown = FALSE,
  label_exclude_categories = NULL,
  label_pattern = NULL,
  max_labels = NULL,
  label_min_gap_deg = NULL,
  label_line_length = 0.02,
  mini_label_line_length = NULL,
  min_feature_bp = 1,
  label_wrap_width = 28L,
  label_anchor_radius = 1.07,
  label_lane_spacing = 0.24,
  label_lane_step = NULL,
  linear_label_offset = 0.14,
  linear_label_lane_step = NULL,
  linear_label_allow_gene_line_crossing = FALSE,
  linear_label_line_angle = 90,
  linear_label_text_angle = 0,
  linear_label_side = "top",
  label_line_angle = NULL,
  label_text_angle = NULL,
  label_line_colour = "grey70",
  label_linewidth = 0.3,
  label_line_linetype = "dashed",
  label_adjust = NULL,
  show_ruler = TRUE,
  ruler_major_bp = NULL,
  ruler_minor_bp = NULL,
  gene_radius = 1,
  gene_height = NULL,
  gene_linewidth = NULL,
  gene_border_linewidth = NULL,
  gene_arrow_head_bp = NULL,
  gene_arrow_head_fraction = NULL,
  linear_row_spacing = 4,
  gene_gap_bp = NULL,
  gc_skew_radius = 0.78,
  gc_skew_height = NULL,
  gc_content_radius = 0.67,
  gc_content_height = NULL,
  gc_content_linewidth = NULL,
  gc_legend_linewidth = 1.8,
  ruler_minor_tick = 0.015,
  ruler_major_tick = 0.035,
  ruler_label_radius = 0.47,
  ruler_linewidth = 0.28,
  ruler_minor_linewidth = 0.18,
  ruler_major_linewidth = 0.38,
  sequence_linewidth = 0.55,
  label_text_size = NULL,
  label_text_colour = "black",
  linear_label_wrap_width = 18L,
  linear_label_max_lines = 2L,
  row_label_text_size = 3.4,
  ruler_text_size = 2.25,
  center_text_size = 4.3,
  legend_position = NULL,
  legend_text_size = 8.5,
  legend_columns = NULL,
  gc_legend_columns = NULL,
  legend_plot_spacing = NULL,
  inner_radius = NULL,
  font_family = "sans",
  gc_window = 100L,
  gc_step = NULL,
  width = NULL,
  height = NULL,
  dpi = 300
)
}
\arguments{
\item{annotation}{A data frame or path to a CSV/TSV annotation table.}

\item{gbk}{Optional GenBank file.}

\item{fasta}{Optional FASTA file for annotation-table input, or as an
alternative sequence source when the GenBank record has no \code{ORIGIN}.}

\item{skew_table}{Optional precomputed GC-skew table with \code{position} and \code{gc_skew}.}

\item{output}{Optional output path. When supplied, the plot is saved and
invisibly returned.}

\item{layout}{Either \code{"circular"} or \code{"linear"}.}

\item{phage_topology}{Either \code{"circular"} or \code{"linear"}. Only used when
\code{layout = "circular"}; \code{"linear"} adds a terminal boundary line.}

\item{name}{Display name.}

\item{genome_length}{Optional plasmid/genome length.}

\item{region_start, region_end}{Optional 1-based inclusive coordinates for
the sequence window to display. Features are filtered and rebased to the
window; sequence-derived GC tracks and FASTA records are sliced to match.}

\item{feature_types}{GenBank feature types to keep.}

\item{label_mode}{\code{"auto"} prefers compact gene labels, \code{"product"} uses
product names, and \code{"gene"} prefers gene IDs.}

\item{palette}{Any palette name exposed by an exported
\code{ggsci::pal_*} function. Examples include \code{"npg"},
\code{"aaas"}, \code{"lancet"}, \code{"jco"}, \code{"locuszoom"}, and
\code{"futurama"}.}

\item{gene_highlight}{Optional named character vector, or data frame with
category/color columns, used to override specific category colors.}

\item{rows}{Number of rows for linear layout.}

\item{genome_line_num, plot_line_num}{Aliases for \code{rows}. When supplied,
\code{genome_line_num} overrides \code{rows}, and \code{plot_line_num} overrides both.}

\item{show_gc_skew}{Whether to draw GC-skew and GC-content tracks when
sequence/skew data are available.}

\item{show_labels}{Whether to draw feature labels.}

\item{label_unknown}{Whether to label unknown/hypothetical features.}

\item{label_exclude_categories}{Optional category names whose features should
still be drawn but not labeled.}

\item{label_pattern}{Optional regular expression. Matching features are
prioritized for labels.}

\item{max_labels}{Maximum number of labels to consider. The default is \code{Inf},
so all eligible labels are considered. Circular labels are placed from
shorter to longer text on farther outer lanes when needed. Use a finite
value to label only the top eligible features.}

\item{label_min_gap_deg}{Minimum angular gap between circular labels. Larger
values omit more crowded labels. Set to \code{0} to disable angular thinning.}

\item{label_line_length}{Distance from the feature anchor to radial labels,
in circular plot radius units.}

\item{mini_label_line_length}{Clearer alias for \code{label_line_length}. If
supplied, it overrides \code{label_line_length}.}

\item{min_feature_bp}{Minimum feature length to label.}

\item{label_wrap_width}{Number of characters per label line before wrapping.
Set to \code{Inf} or \code{NULL} to disable wrapping.}

\item{label_anchor_radius}{Starting radius for outside circular labels.
Increase it to push labels farther from the gene ring.}

\item{label_lane_spacing}{Extra spacing for circular labels when nearby
labels need a small outward offset.}

\item{label_lane_step}{Optional direct radial step between circular label
lanes, in circular plot radius units. Smaller values pack lanes tighter.
When \code{NULL}, the step is calculated from label height.}

\item{linear_label_offset}{Vertical gap between linear gene arrows and the
first label lane.}

\item{linear_label_lane_step}{Optional tuning value for linear label spacing.
This is the distance moved along each connector direction; larger values leave
more space between layers. Linear labels are checked against other labels on
the same genome row and remain below the gene line of the row above.}

\item{linear_label_allow_gene_line_crossing}{Whether a linear label may be
pushed above the gene line of the row above when no free lane remains. The
default \code{FALSE} removes that label and records it in
\code{plasmid_data(plot)$linear_label_summary}.}

\item{linear_label_wrap_width}{Number of characters per line for linear labels
before the maximum-line rule is applied. The default is \code{18} to keep
linear labels compact.}

\item{linear_label_max_lines}{Maximum number of text rows for a linear gene
label. The default is \code{2}; excess wrapped text is folded into the final
row.}

\item{linear_label_line_angle}{Connector-line angle from the gene center to
the label center, in degrees. \code{90} is vertical and \code{45} is diagonal.
It is used when \code{linear_label_text_angle = 0}. For rotated text, labels
are packed rightward first and then upward, so the connector follows the final
label position instead of forcing this angle. The first available label lane
has no connector. When a label is moved to a farther lane, the connector
endpoint and length are recalculated at the near edge of the text.}

\item{linear_label_text_angle}{Rotation angle for linear labels, in degrees. A
nonzero value rotates the text and activates right-first, upward-second label
packing. If that angle cannot place all labels, horizontal and vertical
fallback angles are tested and the best-fitting angle is used. Use \code{0} to
keep text horizontal and use \code{linear_label_line_angle} for the connector.}

\item{label_line_angle, label_text_angle}{Short aliases for the two linear
label-angle parameters. When supplied, they override the corresponding
\code{linear_} values.}

\item{linear_row_spacing}{Vertical distance between adjacent linear genome
rows.}

\item{linear_label_side}{Which side of each linear gene may receive labels:
\code{"top"} (the default) or \code{"both"}. In \code{"both"} mode labels are
packed independently above and below each genome row.}

\item{label_line_colour, label_linewidth, label_line_linetype}{Label leader
line colour, width, and linetype. \code{label_line_colour} may be a fixed R
colour, a vector with one colour per label, or the name of an annotation column
containing valid R colours.}

\item{label_adjust}{Manual label shifts for either layout. Use
\code{\link[=label_adjust]{label_adjust()}}, a data frame with \code{label},
\code{hjust}, and \code{vjust} columns, or a list with those entries. A data
frame may use \code{gene} or \code{gene_id} instead of \code{label}. Positive
\code{hjust} moves a label right and positive \code{vjust} moves it up.}

\item{show_ruler}{Whether to draw the circular clock/ruler track. Only used
when \code{layout = "circular"}.}

\item{ruler_major_bp, ruler_minor_bp}{Optional major/minor ruler tick spacing
in bp. When omitted, sensible values are chosen from genome length.}

\item{gene_radius, gene_height}{Circular gene-ring radius and gene arrow
height. For linear maps, \code{gene_height} controls arrow height.}

\item{gene_linewidth, gene_border_linewidth}{Outline width for gene arrows.
\code{gene_border_linewidth} is a clearer alias; if supplied, it overrides
\code{gene_linewidth}.}

\item{gene_arrow_head_bp}{Optional gene arrow-head length in bp. The default
scales with genome length.}

\item{gene_arrow_head_fraction}{Maximum fraction of each feature that can be
used as the arrow head. When \code{NULL}, circular maps use \code{0.55} and linear
maps use \code{0.35}.}

\item{gene_gap_bp}{Gap removed from adjacent circular feature arrows, in bp.
Use \code{0} for no gap.}

\item{gc_skew_radius, gc_skew_height}{Circular GC-skew track radius and
height.}

\item{gc_content_radius, gc_content_height}{Circular GC-content track radius
and height. For linear maps, \code{gc_content_height} controls line amplitude.}

\item{gc_content_linewidth}{Line width for the GC-content track. When \code{NULL},
the default is \code{0.28} for circular maps and \code{0.45} for linear maps.}

\item{gc_legend_linewidth}{Line width for GC content/skew entries in the
legend.}

\item{ruler_minor_tick, ruler_major_tick, ruler_label_radius}{Circular ruler
tick lengths and label radius.}

\item{ruler_linewidth, ruler_minor_linewidth, ruler_major_linewidth}{Circular
ruler line widths.}

\item{sequence_linewidth}{Linear layout baseline width.}

\item{label_text_size, ruler_text_size, center_text_size, legend_text_size}{Text
sizes.}

\item{label_text_colour}{Label text colour. Use \code{"category"} to use the
selected palette, a fixed R colour such as \code{"black"}, a vector with one
colour per label, or the name of an annotation column containing valid R
colours.}

\item{row_label_text_size}{Text size for linear row labels.}

\item{legend_position}{Legend position passed to ggplot2, such as
\code{"bottom"}, \code{"right"}, \code{"left"}, \code{"top"}, or
\code{"none"}. Use \code{"left_top"} to anchor the legend at the upper-left
above the map without covering it.}

\item{legend_columns}{Number of columns in the feature-category legend. When
omitted, right/left legends use one column and top/bottom legends use
three columns.}

\item{gc_legend_columns}{Number of columns in the GC content/skew legend.
When omitted, right/left legends use one column and top/bottom legends use
three columns.}

\item{legend_plot_spacing}{Space in cm between the plot panel and legend.
When omitted, right/left legends are moved outward based on the measured
outer label range.}

\item{inner_radius}{Inner radius passed to \code{coord_radial()} for circular
maps. Larger values make a larger center hole and push tracks outward.
Use \code{NULL} to choose a layout-aware default.}

\item{font_family}{Font family passed to ggplot2 text themes. The default
\code{"sans"} avoids Windows font database warnings.}

\item{gc_window}{GC-skew window.}

\item{gc_step}{GC-skew step. Use \code{NULL} to choose an automatic step that is
faster for long genomes.}

\item{width, height, dpi}{Output size for \code{output}.}
}
\value{
A ggplot object.
}
\description{
A light wrapper around \code{\link[=ggplasmid]{ggplasmid()}} that uses the phage category scheme.
\code{phage_topology = "linear"} only adds a terminal boundary line when
\code{layout = "circular"}.
}
